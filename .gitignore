node_modules
.DS_Store
dist
dist-ssr
*.local
count.txt
.env
.tanstack
todos.json

# Agent skills live in .claude/skills/ and ARE committed — they're part of
# the project. There is no install step; Claude Code reads that folder
# directly. Don't add an installer back.
